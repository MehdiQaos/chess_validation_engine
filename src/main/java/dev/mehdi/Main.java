package dev.mehdi;

import java.util.Arrays;
import java.util.stream.IntStream;
import java.util.stream.Stream;

public class Main {
    public static void main(String[] args) {
        Chess chess = new Chess();
        Response res = Response.builder().build();
//        int[] numbers = IntStream.rangeClosed(1, 30).filter(n -> n % 3 == 0).filter(n -> n > 3).toArray();
//        for (int n: numbers)
//            System.out.println(n);
//        int[] array = IntStream.rangeClosed(1, 10)
//                .flatMap(n -> IntStream.range(1, n))
//                .distinct()
//                .sorted()
//                .toArray();
    }
}